$( function() {
    'use strict';
    
  /* setTimeout( function(){
        var snapInterval = 1;
         //wheel date calculations
        Draggable.create('#wheel', {
            type:'rotation', 
            throwProps: true,
            liveSnap: function(endValue){
                endValue = (Math.round(endValue / snapInterval) * snapInterval) % 360;
                endValue = endValue < 0 ? endValue + 360 : endValue;
                getRotationValue(endValue)
                return endValue;
            }
           // onDragEnd:calculateDate 
        });
        //get rotation value & calculate date
        function getRotationValue(e){
            console.log(e);
        }
    }, 200);*/
    
});